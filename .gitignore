# Ambiente virtual Python
venv/
.venv/
env/

# Cache Python
__pycache__/
*.pyc
*.pyo
*.pyd

# Configurações e estado local do Claude Code (inclui worktrees internos)
.claude/

# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini

# IDEs
.vscode/
.idea/
*.suo
*.user

# Migração pro Supabase (migration/) — credenciais e artefatos gerados
migration/.env
migration/output/

# WebView2 SDK — binários redistribuíveis de terceiros, baixados manualmente
# (ver Fire Utils.tab/lib/webview2_runtime/README.md)
Fire Utils.tab/lib/webview2_runtime/*.dll

# Build do frontend (webapp/) — gerado por `npm run build`, não versionado
webapp/node_modules/
webapp/dist/
