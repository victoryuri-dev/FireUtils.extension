# Copie este arquivo para migration/.env e preencha com as credenciais do
# seu projeto Supabase (Settings > API). migration/.env NUNCA deve ser
# commitado — já está no .gitignore.

SUPABASE_URL=https://SEU-PROJETO.supabase.co
SUPABASE_SERVICE_ROLE_KEY=coloque-aqui-a-service-role-key
